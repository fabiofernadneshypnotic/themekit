{% assign variable = 5 | plus: 5 %}
{{ variable | plus: 5 }}

{% assign array_string = "Fábio,Miguel,Tiago,Guilherme,Ana,Joao" %} {% assign
array = array_string | split: "," %}

<ul>
  {% for name in array %} {% if name.size < 4 %}
  <li>{{ name }}</li>
  {% endif %} {% endfor %}
</ul>

{% assign collections = collections['all'] %} {% for product in
collections.products %}

{{ product.title }} - {{ product.price | money }} - {% if product.available !=
true %} {SOLD OUT} {%endif%} {% endfor %}
